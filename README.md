# Hibby's Packet Radio Guide

To use this as a website instead of flat text files, install `mkdocs` and `mkdocs-material`

On Debian based distros, `apt install mkdocs-material mkdocs` should do it.

